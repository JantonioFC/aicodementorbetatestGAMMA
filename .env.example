# AI CODE MENTOR - VARIABLES DE ENTORNO EJEMPLO
# Copiar a .env.local y configurar valores reales

# === GEMINI API CONFIGURATION ===
# Obtener API Key en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=tu_gemini_api_key_aqui

# Modelo Gemini a utilizar (CONFIRMADO disponible en API v1beta 2025-09-27)
# Opciones CONFIRMADAS disponibles en API v1beta:
# - gemini-2.5-flash: Modelo estable, mejor precio-rendimiento (RECOMENDADO)
# - gemini-2.5-pro: Modelo más avanzado para tareas complejas
# - gemini-2.0-flash: Modelo estable de generación anterior
# - gemini-flash-latest: Apunta al modelo Flash más reciente
# NOTA: Los modelos gemini-1.5-* NO están disponibles en API v1beta
GEMINI_MODEL_NAME=gemini-2.5-flash

# Modelo Gemini para cliente (debe coincidir con el del servidor)
NEXT_PUBLIC_GEMINI_MODEL_NAME=gemini-2.5-flash

# === SUPABASE CONFIGURATION ===
# Sistema de autenticación y base de datos
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_clave_publica_anonima
SUPABASE_SERVICE_ROLE_KEY=tu_clave_de_servicio

# === MICROSERVICIO IRP CONFIGURATION ===
# Cliente IRP para comunicación inter-servicios
# URL del Microservicio IRP (Puerto 3001)
IRP_SERVICE_URL=http://localhost:3001/api/v1

# Configuración de timeouts y reintentos para cliente IRP
IRP_TIMEOUT_MS=10000
IRP_MAX_RETRIES=3
IRP_RETRY_DELAY_MS=1000

# JWT Secret compartido con microservicio IRP (debe coincidir con .env del microservicio)
IRP_JWT_SECRET=tu_secreto_jwt_compartido_minimo_32_caracteres

# Configuración de notificaciones para webhook IRP
IRP_WEBHOOK_ENABLED=true
IRP_NOTIFICATION_TIMEOUT_MS=5000

# === CONFIGURACIÓN ADICIONAL ===
# Configuraciones opcionales para desarrollo
NODE_ENV=development

# === SENTRY MONITORING ===
NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_AUTH_TOKEN=tu_auth_token_aqui
